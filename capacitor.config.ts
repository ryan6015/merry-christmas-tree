import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wsj.merry.christmas.tree.app',
  appName: 'merry-christmas',
  webDir: 'dist'
};

export default config;
